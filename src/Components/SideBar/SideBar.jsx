import { Box } from '@mui/material';
import React from 'react';

const SideBar = () => {
    return (
        <Box sx={{
            width:"25vw",
            height:"100vh",
            bgcolor:"background.paper",
            
        }}>
            
        </Box>
    );
};

export default SideBar;